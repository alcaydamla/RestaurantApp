<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="#6C78BA"> <!-- Gri renk efekti -->

    <!-- Kare şekli oluştur -->
    <item android:id="@android:id/mask">
        <shape android:shape="rectangle">
            <solid android:color="#6C78BA" />
            <corners android:radius="2dp" /> <!-- Oval Kenarlar -->
        </shape>
    </item>

</ripple>
